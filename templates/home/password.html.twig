{% extends 'base.html.twig' %}

{% block title %}Modification du Password{% endblock %}

{% block body %}
{{ form_start(form) }}
    <h3>Modifiez votre Mot de Passe</h3>

    <div class=" text-start align-item">
            

        <div class="d-flex flex-row text-start align-items">
        <div class="col-md-6 m-3 d-none">
            {{ form_row(form.firstname) }} 
        </div>

        <div class="col-md-3 m-3 d-none">
            {{ form_row(form.lastname) }}
        </div>

        <div class="col-md-3 mx-3 my-2 d-none">
            {{ form_row(form.birthdate) }}
        </div>

        <div class=" col-md-1 mx-3 my-2 d-none">        
            {{ form_row(form.roles) }}
        </div>
    </div>

    <div class="d-flex flex-row text-start align-item">
        <div class="col-md-3 m-3 d-none">
            {{ form_row(form.email) }} 
        </div>

        <div class="col-md-3 m-3">
          {{ form_row(form.password) }}
        </div>

        <div class=" col-md-3 m-3 d-none">        
          {{ form_row(form.picture) }}
        </div>
        
        {# <div class=" col-md-3 mb-3">        
          {{ form_row(form.city) }}
        </div>
        <div class=" col-md-3 mb-3">        
          {{ form_row(form.club) }}
        </div> #}
    </div>
    
    <div class = "text-center mx-3">
        <button class="btn btn-success btn-lg my-5 " >{{ button_label|default('Enregistrer') }}</button>
    </div>
{{ form_end(form) }}
    <a href="{{ path('homepage') }}">retour à l'accueil</a>
{% endblock %}