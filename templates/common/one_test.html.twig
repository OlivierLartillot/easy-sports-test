{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('userTeam') }}
{% endblock %}
<div class="container">
    <h1 class="title-page name_test">Test : {{test.name}} (en {{test.unit}})</h1>

    <div class="row">
        <div class="col-12 col-md-6">
                <p id="description_test">{{test.description}}</p>
                {{ form_start(form) }}
                {{ form_row(form.team) }}    
                {{ form_row(form.user) }} 
                {{ form_row(form.result) }} 
                <button class="btn">{{ button_label|default('Save') }}</button>
                {{ form_end(form) }} 
        </div>
        <div class="p-2 col-12 col-md-6">
        {% if test.media != null %}
            <img src="{# {{ asset(  test.media ) }} #}{{asset('uploads/images/tests/' ~ test.media)}}" width="100%" id="test_img">
                <video class="d-none"id="test_video" width="100%"  controls>
                    <source  type="video/mp4">
                </video>
        {% endif %}
        </div>
    </div>
</div>
        

   
    <a href="{{ path('user_home', {slug : app.user.slug}) }}">retour à l'accueil</a>



    </div>
{% endblock %}
